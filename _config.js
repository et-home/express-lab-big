const db = {
  host: "localhost",
  user: "uername",
  password: "password",
  database: "database",
  connectionLimit: 5,
};

const config = {};

config.db = db;

module.exports = config;
